<?xml version="1.0" encoding="utf-8"?>
<!-- fragment_screen_slide_page.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/room_page_content"
    android:background="#c8c8c8DD"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <GridView xmlns:android="http://schemas.android.com/apk/res/android"
        android:layoutAnimation="@anim/row_animation"
        android:id="@+id/image_gridview"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:numColumns="2"/>

    <!--android:padding="10dp"
    android:horizontalSpacing="10dp"
    android:verticalSpacing="10dp"
    android:numColumns="2"-->

   <!-- <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@drawable/alpha_text_view"
        android:layout_alignParentBottom="true"
        android:gravity="center_horizontal">

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/cocoreation_room_add_room"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@android:drawable/ic_input_add"
            android:padding="0dp" />

    </LinearLayout>-->

    <android.support.design.widget.BottomNavigationView
        android:id="@+id/room_list_bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/colorPrimary"
        app:menu="@menu/bottom_bar_room_list"
        app:itemIconTint="#FFFFFF"
        app:itemTextColor="#FFFFFF" />

</RelativeLayout>